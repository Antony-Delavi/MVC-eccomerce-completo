// Exemplo de lógica organizada
async function createOrder(userId, items) {
  // 1. Verificar se os produtos existem
  // 2. Validar se há estoque disponível
  // 3. Calcular o preço total
  // 4. Salvar no SQLite dentro de uma Transaction (segurança de dados)
  // 5. Baixar o estoque dos produtos
}